import Image

class DrawrBrushes():

    def __init__(self):
        self.brushes = []
        self.brush_names = ["circle", "cathead", "goblin", "kappa"];
        self.brush_sizes = [16, 16, 16, 32];

        for i in range(0, len(self.brush_names)):
            src = #something
            im = Image.open(src) # http://effbot.org/imagingbook/introduction.htm


image.paste(something, (0,0,100,100))
